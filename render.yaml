services:
  - type: web
    name: ships-frontend
    env: node
    buildCommand: corepack enable && npm install && npm run build
    startCommand: npm run preview
    envVars:
      - key: VITE_API_URL
        value: https://ships-backendmoi-k6ob.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html 